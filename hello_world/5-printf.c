#include <stdio.h>
/**
 * main - This is to demostrate the printf function in the
 * stdio.h library by having new lines after every comma
 * using the \n code in the string.
 *
 * Return: 0 as it is a simple excerise of running
 * functions for beginner programming - always SUCCESS
 */

int main(void)
{
	printf("with proper grammar,\n but the outcome is a piece of art,\n");
	return (0);
}
